import React from 'react';
import { IGraphProps } from '../../util/interfaces';

import './Graph.scss';

export default function Graph(props: IGraphProps) {
  return (
    <span className="graph-wrapper">
    </span>
  )
}